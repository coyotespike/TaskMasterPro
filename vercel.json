{
  "version": 2,
  "buildCommand": "cp package.json.nextjs package.json && npm install && npm run build",
  "devCommand": "npm run dev",
  "outputDirectory": "out",
  "framework": null,
  "env": {
    "NODE_ENV": "production"
  }
}